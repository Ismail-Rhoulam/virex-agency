import { home, about, services, portfolio, socials } from './content';

const render = () => {
    return {
        home,
        about,
        services,
        portfolio,
        socials
    }
};

export { render }